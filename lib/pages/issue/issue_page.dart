import 'package:flutter/material.dart';

class IssuePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('This should be the Issues'));
  }
}
